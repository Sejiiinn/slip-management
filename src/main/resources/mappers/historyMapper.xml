<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="sejin.slipmanagement.repository.HistoryDAO">
    <select id="findAll" resultType="sejin.slipmanagement.domain.History">
        SELECT people,place,count,expense FROM history
    </select>
</mapper>